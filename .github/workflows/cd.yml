---
    name: Push docker image to Docker Hub

    on:
      push:
        branches:
          - master

    jobs:
        build:
            runs-on: ubuntu-latest
            steps:
                - name: Checkout code
                  uses: actions/checkout@v2
                - name: Setting up docker buildx
                  uses: docker/setup-buildx-action@v3
                - name: Login to DockerHub
                  uses: docker/login-action@v3
                  with:
                    username: ${{ secrets.USERNAME }}
                    password: ${{ secrets.DOCKER_PASSWORD }}
                - name: Build and push
                  uses: docker/build-push-action@v6
                  with:
                    push: true
                    tags: |
                        ${{ secrets.USERNAME }}/bun-app:latest
                        ${{ secrets.USERNAME }}/bun-app:${{ github.sha }}
                    cache-from: type=registry,ref=${{ secrets.USERNAME }}/bun-app:buildcache
                    cache-to: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/bun-app:buildcache,mode=max